export enum colorScheme {
  Light = 'light',
  Dark = 'dark',
}

export enum langSwitcher {
  LangRu = 'ru',
  LangEn = 'en',
}

export enum Paths {
  Home = '/',
  Login = '/login',
  Register = '/register',
  Dashboard = '/dashboard',
}

export enum Types {
  Login = 'login',
  Register = 'register',
}

export enum Routes {
  Home = 'Home',
  Login = 'Login',
  Register = 'Register',
}
